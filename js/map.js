function main() {
    document.addEventListener('keyup', function (e) {
        handleKey(e.key);
    })
}

function handleKey(key) {
    switch (e.key) {
        case 'w':

            break;
        case 'a':

            break;
        case 's':

            break;
        case 'd':
            s
            break;
    }
}